{
  "name": "backend",
  "version": "1.0.0",
  "description": "Esta estructura separa la aplicación en las tres capas clave: **Dominio**, **Aplicación**, y **Infraestructura/Adaptadores**, garantizando Inversión de Dependencias (DIP) y testabilidad.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/index.js",
    "dev": "tsc --watch & node --watch dist/index.js",
    "start:prod": "npm run build && npm run start",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
